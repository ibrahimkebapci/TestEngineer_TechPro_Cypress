describe('ilk Testim', () => 
{
    it('true, true ile eşit mi!', () =>
    {
        expect(true).to.equal(true)
    })

    it('true, true ile eşit mi!', () =>
    {
       //Intellisense
       cy.get('').click();
       cy.get('').find('');
    })
})


describe('İlk Testim', () => {
    it('true, true ile eşit mi!' , () => {
    expect(true).to.equal(true)
    // 1. Kullanıcı URL ' gider
    // 2. Login butonuna tıklanır
    // 3. Geçerli bir user name girelir
    // 4. GEçerli bir şifre girer
    // 5. Login butonuna tıklar
    // 6. Login işleminin başarılı olduğu doğrulanır
    })
    

   })